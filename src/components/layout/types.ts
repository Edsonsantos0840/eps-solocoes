export interface AddressData {
    cidade: string;
    estado: string;
};

export interface ContactData {
    telefone: string;
    email: string;
};

export interface LinksProps {
    link: string,
    text: string
}